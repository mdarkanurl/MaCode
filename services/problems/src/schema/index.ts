import { createProblemsSchema, getProblemSchema, submitSolutionSchema } from "./problems-schema";

const problemsSchema = {
    createProblemsSchema,
    getProblemSchema,
    submitSolutionSchema
}

export {
    problemsSchema
}